import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  font-size: 0.14rem;
  color: #ffffff;
  line-height: 0.4rem;
`;

const Header = styled.div`background-color: #0889FF；;`;

const Desc = () => (
  <Container>
    <Header>123</Header>
  </Container>
);

export default Desc;
