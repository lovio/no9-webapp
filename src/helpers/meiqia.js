/* eslint-disable */
export function showPanel() {
  _MEIQIA('showPanel');
  // // clientId怎么传呢？。。。暂且写入全局变量吧
  // let clientID = 'zhan-toefl';
  // if (window.clientID) {
  //   // should be less than 32 bits
  //   clientID = window.clientID.replace(/-/g, '');
  // }
  // console.log(clientID);
  // location.href = `https://static.meiqia.com/dist/standalone.html?eid=21064&clientid=${clientID}`;
}
